package main

func main() {
	// app := sraph.New(sraph.AppOptions{
	// 	Ctx: context.Background(),
	// })
	// window := app.NewWindow(sraph.WindowOptions{})

	// // window.Run()

	// if err := app.Run(); err != nil {
	// 	panic(err)
	// }
}
